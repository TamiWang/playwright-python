[pytest]
testpaths = tests
addopts = --html=reports/report.html --self-contained-html -v
markers =
    smoke: quick tests that verify core functionality
    regression: full regression suite
    slow: long-running test

